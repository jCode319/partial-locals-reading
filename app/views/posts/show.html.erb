Information About the Post
<% render partial: "authors/author", locals: {post_author: @post.author} %>
  #telling Rails where to render               #hash key's creates a local variable & p_a is the argument name
<%= @post.title %>
<%= @post.content %>
